services:
  - type: worker
    name: telegram-bot-hyperliquid
    env: python
    plan: free
    branch: main
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TOKEN
        fromDatabase: true  # o pon el valor aquí directamente si quieres
